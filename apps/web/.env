# Local development defaults — matches docker-compose.yml. No real secrets.
# Production DATABASE_URL (Neon) is set in Vercel project env vars, never here.
DATABASE_URL=postgres://wefood:wefood@localhost:5435/wefood

# Kept out of Vercel's way (a platform PORT is injected there); local-only default.
PORT=3010
